name: "Format and create PR"

on:
    workflow_dispatch:
        inputs:
            mode:
                description: "Hvilken jobb som skal kjøres."
                type: choice
                required: true
                default: ruff-format
                options:
                    - ruff-format
                    - blame-ignore-revs

permissions:
    contents: write
    pull-requests: write

concurrency:
    group: format-and-create-pr-${{ inputs.mode }}
    cancel-in-progress: false

jobs:
    format:
        uses: hwinther/reusable-workflows/.github/workflows/format-and-create-pr.yml@3ad0a22859d540e81f210707b744607321b5ae1b # v1.55.0
        with:
            mode: ${{ inputs.mode }}
            commit_subject: "chore: ruff format"
            python_version: "3.13"
            ruff_version: "0.15.12"
            target_path: "."
