name: PR build

on:
    pull_request:
        branches:
            - main
    workflow_dispatch:

concurrency:
    group: pr-build-caller-${{ github.ref }}
    cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
    contents: read
    pull-requests: write
    checks: write
    packages: read

jobs:
    pr-build:
        uses: hwinther/reusable-workflows/.github/workflows/pr-build.yml@81d8764d1f0c6a18d1e22452f137376c0ec6d023 # v1.59.0
        with:
            python_enabled: true
            python_version: "3.13"
            python_changes_regex: '^(src/|scripts/|pyproject\.toml|\.github/workflows/pr-build\.yml)'
