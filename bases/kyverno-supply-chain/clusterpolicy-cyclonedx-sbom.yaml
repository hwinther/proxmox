# Requires a CycloneDX SBOM attestation in the registry (cosign attest --type cyclonedx from the build).
# Identity matches hwinther/reusable-workflows docker-container reusable workflow @ v1 (see clusterpolicy-github-slsa-provenance).
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: wsh-require-cyclonedx-sbom
  annotations:
    policies.kyverno.io/title: Require CycloneDX SBOM attestation (ghcr.io/hwinther)
    policies.kyverno.io/category: Supply Chain
    policies.kyverno.io/severity: medium
spec:
  validationFailureAction: Audit
  background: false
  # Kubernetes caps webhook timeout at 30s.
  webhookTimeoutSeconds: 30
  rules:
  - name: cyclonedx-attestation
    match:
      any:
      - resources:
          kinds:
          - Pod
    exclude:
      any:
      - resources:
          namespaces:
          - kube-system
          - kyverno
          - flux-system
    verifyImages:
    - imageReferences:
      - ghcr.io/hwinther/*
      failureAction: Enforce # Note: Enforce is required for mutateDigest: true
      mutateDigest: true
      verifyDigest: true
      attestations:
      - predicateType: https://cyclonedx.org/schema
        attestors:
        - entries:
          - keyless:
              issuer: https://token.actions.githubusercontent.com
              subjectRegExp: ^https://github.com/hwinther/reusable-workflows/\.github/workflows/docker-container\.yml@refs/tags/v1$
              rekor:
                url: https://rekor.sigstore.dev
        conditions:
        - all:
          - key: "{{ Data.bomFormat }}"
            operator: Equals
            value: CycloneDX
