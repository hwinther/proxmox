# Platform namespace for edge-sdr (e.g. Homepage on controller / non-Pi workers).
# Pi pool uses role=sdr-edge + taint node-type=edge-sdr; those nodes are excluded from Homepage scheduling.
apiVersion: v1
kind: Namespace
metadata:
  name: platform-edge-sdr
