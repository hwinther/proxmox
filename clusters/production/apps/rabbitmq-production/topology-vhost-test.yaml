# Shared vhost for test / non-prod workloads (not per-app). Default queue type: quorum.
apiVersion: rabbitmq.com/v1beta1
kind: Vhost
metadata:
  name: test
  namespace: rabbitmq-production
spec:
  name: test
  defaultQueueType: quorum
  rabbitmqClusterReference:
    name: rabbitmq
