# Shared vhost for production workloads (not per-app). Default queue type: quorum.
apiVersion: rabbitmq.com/v1beta1
kind: Vhost
metadata:
  name: prod
  namespace: rabbitmq-production
spec:
  name: prod
  defaultQueueType: quorum
  rabbitmqClusterReference:
    name: rabbitmq
