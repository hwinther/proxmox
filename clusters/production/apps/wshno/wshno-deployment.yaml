apiVersion: apps/v1
kind: Deployment
metadata:
  name: wshno
  namespace: wshno-production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wshno
  template:
    metadata:
      labels:
        app: wshno
        app.kubernetes.io/name: wshno
    spec:
      automountServiceAccountToken: false
      securityContext:
        # No runAsUser/fsGroup — the upstream image (nginxinc/nginx-unprivileged:alpine)
        # already declares `USER nginx` (UID 101) and chowns its writable dirs accordingly.
        # runAsNonRoot is enough to satisfy the Kyverno wsh-require-run-as-non-root policy.
        runAsNonRoot: true
      containers:
      - name: wshno
        image: ghcr.io/wshno/wshno/web:0.6.0@sha256:9c5674b59ec489c1712e59a3e69366e2444e96f46e2c3030a1469c2795c88df8
        imagePullPolicy: IfNotPresent
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        resources:
          requests:
            cpu: 25m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 64Mi
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        readinessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 3
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 30
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 6
---
apiVersion: v1
kind: Service
metadata:
  name: wshno
  namespace: wshno-production
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: wshno
