# Clone CNPG application Secret into clutterstock-production for secretKeyRef on the API Deployment.
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: clone-postgres-prod-app-secret-to-clutterstock-production
  annotations:
    policies.kyverno.io/title: Clone postgres-prod-app secret to clutterstock-production
    policies.kyverno.io/category: CNPG
    policies.kyverno.io/severity: low
spec:
  generateExisting: true
  rules:
  - name: clone-postgres-prod-app
    match:
      any:
      - resources:
          kinds:
          - Secret
          namespaces:
          - postgres-production
          names:
          - postgres-prod-app
    generate:
      apiVersion: v1
      kind: Secret
      name: postgres-prod-app
      namespace: clutterstock-production
      synchronize: true
      clone:
        namespace: postgres-production
        name: postgres-prod-app
