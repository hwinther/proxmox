# Bump metadata.name when changing template or migrator image (Job spec is immutable).
apiVersion: batch/v1
kind: Job
metadata:
  name: clutterstock-migrate-v108
  namespace: clutterstock-production
spec:
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/component: clutterstock-migrate
    spec:
      automountServiceAccountToken: false
      restartPolicy: Never
      securityContext:
        runAsNonRoot: true
        runAsUser: 1654
        fsGroup: 1654
        fsGroupChangePolicy: OnRootMismatch
      containers:
      - name: migrator
        image: ghcr.io/hwinther/clutterstock/migrator:0.22.0
        imagePullPolicy: Always
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        env:
        # Connection string assembled from individual fields: clutterstock-pg-secret only has a usable
        # `password` (its `uri` key carries an unsubstituted `<password>` placeholder).
        - name: PG_HOST
          value: postgres-prod-rw.postgres-production.svc.cluster.local
        - name: PG_DB
          value: clutterstock
        - name: PG_USER
          value: clutterstock
        - name: PG_PASSWORD
          valueFrom:
            secretKeyRef:
              name: clutterstock-pg-secret
              key: password
        - name: ConnectionStrings__ClutterStock
          value: "Host=$(PG_HOST);Port=5432;Database=$(PG_DB);Username=$(PG_USER);Password=$(PG_PASSWORD)"
        resources:
          requests:
            cpu: 50m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
